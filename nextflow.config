nextflow.enable.moduleBinaries = true
process {
    withName:FASTLMM {
        container = 'image_fastlmm_plink:latest'
    }
    withName:CALCULATE_MAF {
        container = 'image_fastlmm_plink:latest'
    }
}

docker {
    enabled = true
}